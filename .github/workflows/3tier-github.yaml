name: Enterprise DevSecOps CICD Pipeline
on:
  push:
    branches:
      - main
    paths:
      - client/**
      - server/**
      - .github/workflows/qa-cicd.yml
permissions:
  contents: write
  id-token: write
